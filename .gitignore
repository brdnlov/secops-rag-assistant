# Python
__pycache__/
*.pyc
.venv/
venv/

# Eval artifacts (regenerated by harness)
eval/results/

# OS
.DS_Store
Thumbs.db

# Secrets (never commit)
.env
*.env

# Local agent/session working files (not part of the project)
AGENTS.md
session-summaries/

# Runtime run logs (indexer/embedder job artifacts, regenerated on each run)
embeddings/*.log
embeddings/*.err

# Runtime query traces (regenerated on each query; keep the dir, ignore the data)
logs/traces.jsonl
logs/_layer3_traces.jsonl