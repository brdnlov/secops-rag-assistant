tiktoken>=0.14.0
python-dotenv>=1.0.0
voyageai>=0.5.0
qdrant-client>=1.19.0
sentence-transformers>=3.0.0

# generation + eval
anthropic>=0.40.0

# RAGAS side-by-side eval (Phase 6).
# ragas 0.2.15 is INCOMPATIBLE with the langchain 1.x line (its
# ragas/llms/base.py imports langchain_community.chat_models.vertexai,
# removed in community 0.4/1.x). Pin the whole langchain 0.3 line together so
# a fresh `pip install -r requirements.txt` does not re-break RAGAS.
ragas>=0.2.15,<0.3
langchain>=0.3,<0.4
langchain-community>=0.3,<0.4
langchain-core>=0.3,<0.4
langchain-openai>=0.3,<0.4
langchain-anthropic>=0.3,<0.4

# api (Phase 5/7)
fastapi>=0.110
uvicorn[standard]>=0.29

# dev / testing
pytest>=8.0.0